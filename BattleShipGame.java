public class BattleShipGame
{
	//Must run BattleShipBoard class
	//Must keep score based on number of guesses
	//If same coordinates guessed twice, turn counts as 2 turns
	//User must be told how many ships are hidden
	//Game is over when all ships are sunk
	//Score must be revealed to user
	//"hit" "miss" "close" "very close"
	
}